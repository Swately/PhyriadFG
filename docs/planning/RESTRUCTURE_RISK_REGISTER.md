# RESTRUCTURE arc — risk register

Tier-2 register for [`RESTRUCTURE_PLAN.md`](RESTRUCTURE_PLAN.md) (PLAN_TIER_PROTOCOL §2). Every
risk starts `open`; a stage of the arc MUST NOT be committed while a risk scoped to that stage is
`open`. Status values: `open` → `mitigated` (code in + verification actually run) or `accepted`
(residual stated + operator sign-off). MUST/MUST NOT are BCP-14.

Scope note: line numbers reference the tree at commit `c043780` (+ the operator's uncommitted
`cli.hpp` default change); they shift as stages land — each stage's commit SHOULD refresh the
sites it moved.

| ID | Class | Stage | Failure mode (exact, with site) | Mitigation + attached code | Verification | Status |
|---|---|---|---|---|---|---|
| CR1 | concurrency | E2, E3 | A lambda→function conversion captures BY VALUE what the `[&]` lambda captured by reference. Example class: `wap_warp_present` (`present.cpp:879–1402`) reads loop-local pacing/EMA state and writes WAP images each tick; a copied scalar (e.g. an EMA accumulator) silently freezes adaptation; a copied handle/pointer struct desyncs the SPSC contract between P and F (`fg_context.hpp` seq/ring fields). Compiles clean, corrupts silently. | Call-site structs whose data members are ALL references, built from the verified capture inventory (I2/I3):<br>`struct WapWarpCall { float& mass_err_ema; uint64_t& plog_c; Img& wap_prev; /* …one `T&` per I2 row… */ };`<br>A by-value member cannot bind to the pattern; the struct definition is reviewed field-by-field against the inventory table, and scalars that are genuinely read-only per call are passed as function parameters, never struct members. | (a) Field-by-field review: every struct member traced to an I2/I3 row, both directions (no extra, no missing). (b) G2/G3 A/B: 60 s `ball_zoo.ps1` before/after, 2 runs per side; uniq/s and `lat` deltas within the measured run-to-run spread. (c) 120 s smoke to clean exit. | `open` |
| CR2 | crash | E1 | The init-seq's sections have hidden cross-section ordering dependencies (e.g. `use_wap`/`have_igpu` computed in "Devices" 845–1073 gate later pipeline sections; `cap_slots` from 1080–1102 sizes rings allocated later). Reordering or re-scoping a moved section leaves a consumer reading an unset value → `VK_ERROR_INITIALIZATION_FAILED` at best, a null-handle crash or device-lost at worst. | Extraction preserves execution order 1:1: `main()` calls `init_*` functions in the exact current section order; each function's ownership struct is passed to the later ones that read its outputs — the dependency becomes a visible parameter instead of a shared scope:<br>`DevicesInit dv = init_devices(cfg, inst);`<br>`ImagesInit im = init_images(cfg, dv);  // reads dv.use_wap, dv.A/B/G` | (a) The moved region is verbatim under `git diff --color-moved=zebra`. (b) Build green ×2 (`build.bat` + `build-release.bat`). (c) 120 s smoke `--monitor 0 --exit-after 120` to clean exit with the same startup log lines (captured before/after, diffed). | `mitigated` (2026-09-01) — G1 ran: builds ×2 exit 0; startup-log diff vs baseline = 0 lines (init order provably 1:1); verbatim check computed = 98.37 % byte-identical, remainder all documented transforms; 120 s smoke exit 0. |
| FR1 | crash | E1 (exclusion) | Teardown (`main.cpp` after the join) destroys resources in inverse init order across ALL sections; splitting teardown per-module breaks the inverse-order interleaving (e.g. a VK image destroyed after its device) → shutdown crash or validation storm. | Teardown is OUT OF SCOPE for E1 (plan §3-E1): it stays a single block in `main()`, reading the ownership structs. The mitigation is the scope exclusion itself; a later arc MAY revisit with per-struct RAII. | G1's smoke reaches *clean exit* (exit code 0, no fault) — the teardown path actually runs in the gate. | `mitigated` (2026-09-01) — teardown stayed in `main()` as specified (main.cpp 1059–1222 untouched in the diff); three smoke runs (25 s Phase-A, 120 s, 30 s post-CRLF) all reached "bounded-run clean exit" with exit code 0. |
| PR1 | dogma (max-performance) | E2, E3 | De-inlining: code moved out of the thread body's TU stops inlining, adding per-tick call overhead in the OUTPUT-CLOCK loop (`present.cpp` ≈1494→) — a regression the max-performance dogma forbids. `ARCHITECTURE.md` claims LTO makes the split perf-free; that claim is adopted, not re-measured. | LTO/IPO already ON (`CMakeLists.txt:117`, `/GL + /LTCG`, message confirmed in the 2026-08-29 build log). The extracted functions stay non-static, single-caller — MSVC LTCG's single-call-site inlining covers them. No `__forceinline` is added preemptively (no speculative layers). | The G2/G3 A/B IS the measurement: if uniq/s or `lat` degrade beyond the run-to-run spread, the stage does not pass until the regression is found or the extraction reverted. DI-3: two runs per side, the spread reported with the verdict. | `open` |
| PR2 | dogma (no speculative layers) | E4 | Variant V-A (generator) adds build tooling (a codegen step + a generated-file discipline) that outlives its value if the drift class it kills was cheap to check — tooling debt. | Plan §3-E4 RECOMMENDS V-B (a standalone `tools/check_flag_sync.py` verifier, no build-graph change) and gates V-A behind recurrence: V-A is only entered if drift recurs AFTER V-B is in place. | The decision is recorded at E4 entry with the I4 triage attached; if V-A is chosen, the generated model MUST be diff-reviewed against the hand model once, before replacement. | `open` |
| DR1 | data (plan integrity) | E0→all | The E0 inventories are light-model output (SUBAGENT_DELEGATION R2: claims, not facts). An inventory error propagates into E1/E2/E3 binding structs — a missing capture row becomes a missing parameter (compiler catches) or a wrongly-classified one becomes a by-value slip (CR1's silent class). Observed already: A1's table omits ranges 501–622 and 1220–1334 (holes found by the supervising session on receipt). | G0 spot-verification ≥5 samples per inventory, BOTH directions (listed row → confirmed in source; source region → confirmed present in inventory), done first-hand by the supervising session before any stage consumes the inventory. Holes found → the region is inventoried first-hand, not by re-delegation. | The spot-check results (sample, expected, found) are recorded in `RESTRUCTURE_INVENTORY.md` §0 alongside each inventory's provenance (model, date). | `open` — the G0 half RAN 2026-08-31 (I1: 2 defects found + corrected; I2/I3/I4: 5/5 samples passed; I4 line citations flagged unreliable). The re-derive-at-consumption half still binds E1–E3, so the risk stays open until those stages close it. |
| DR2 | data (behavior surface) | E4 | The argv round-trip gate could pass while `--help` text drifts (help is hand-written prose in `print_help`, `cli.cpp:`~1020–1126 region of the pre-split file) — a silent doc/behavior mismatch shipped to users. | G4 requires BOTH: byte-identical `--help` AND the round-trip. Two independent instruments for one claim (CONDUCT §3.9) — neither alone passes the gate. | `phyriad_fg.exe --help > before.txt` / `after.txt`, `fc /b` byte compare, output quoted in the gate report. | `open` |

## Carried into the CONVERGENCE arc (2026-09-03)

The layer-contract search closed (`aap/AT3_AT4_ATF_VERDICTS.md`) and the stages that would have exercised
three rows moved arcs: **CR1** → `CONVERGENCE_MASTER_PLAN.md` C4 (the phase-clock port; strategy X11),
**PR1** → C3a (the shader/push replacement; X7/X8, measured by G-C3a's A/B), **DR2** → C0 (the generated
help; X1/X2, together with the new XR6). Their status HERE stays `open` — a risk closes in the arc whose
gate actually runs its Verification, never by relocation. CR2/FR1 (E1) are closed above; PR2/DR1 keep
their E4/E0 scope.

## Linked documents

- Plan: [`RESTRUCTURE_PLAN.md`](RESTRUCTURE_PLAN.md) (tier declaration + stage scopes + gate ladder).
- Inventory: `RESTRUCTURE_INVENTORY.md` (E0 deliverable; carries provenance + G0 spot-checks).
- Discipline sources: `F:\Phyriad\protocols\quality\PLAN_TIER_PROTOCOL.md` §2–§3,
  `F:\Phyriad\protocols\delegation\SUBAGENT_DELEGATION_PROTOCOL.md` R2/R5,
  `F:\Phyriad\protocols\quality\DATA_INSPECTION_PROTOCOL.md` DI-3 (the A/B two-run rule).
